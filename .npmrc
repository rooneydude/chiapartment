# better-sqlite3's prebuilt binaries target Node-API 10, which only exists in
# Node >=22.14. On older runtimes install succeeds and the process then dies
# with an uncatchable segfault, so the engines range is enforced here rather
# than left as an advisory warning.
engine-strict=true
